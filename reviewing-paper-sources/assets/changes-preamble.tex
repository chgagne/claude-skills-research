% ----------------------------------------------------------------------------
% ANNOTATED REVIEW COPY -- do not submit this file.
%
% Insert after the last \usepackage of main-annotated.tex (typically after
% xcolor). Change the author id/name/colour to match the reviewer.
%
%   \chadded{...}          proposed insertion
%   \chdeleted{...}        proposed deletion
%   \chreplaced{new}{old}  proposed rewrite
%   \chcomment{...}        margin note (keep under ~15 words)
%   \chhighlight{...}      flagged passage -- AVOID in two-column layouts,
%                          it paints across the column boundary
%   % CL: ...              reviewer rationale, LaTeX comment only
%
% To produce a clean PDF with every suggestion ACCEPTED:
%     \usepackage[final,commandnameprefix=always]{changes}
% To see the markup (default):
%     as below
%
% commandnameprefix=always is deliberate: document classes and other packages
% commonly define \comment and \highlight, and the unprefixed names collide.
% ----------------------------------------------------------------------------
\usepackage[commandnameprefix=always,todonotes={textsize=tiny,color=yellow!30}]{changes}
\definechangesauthor[name={Claude}, color=blue]{CL}
\setlength{\marginparwidth}{1.6cm}
